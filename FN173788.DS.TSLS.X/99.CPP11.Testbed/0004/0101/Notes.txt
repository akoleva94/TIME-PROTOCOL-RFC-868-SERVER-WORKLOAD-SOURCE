ПУ по "Разпределени системи"

Пример за многозадачно приложение 
[с използване на C++11 Standard Library]

Изследване:
Ефект от фоновото изпълнение чрез нишка (nonblocking mode) 
в сравнение с последователна реализация (blocking mode).
